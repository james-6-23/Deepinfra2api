# DeepInfra API 反封锁配置示例
# 复制此文件为 .env 并根据需要修改

# 基础配置
PORT=8000

# 多镜像端点配置（逗号分隔）
# DEEPINFRA_MIRRORS=https://api.deepinfra.com/v1/openai/chat/completions,https://api2.deepinfra.com/v1/openai/chat/completions

# 代理配置（如果你有代理服务器）
# HTTP_PROXY=http://proxy-server:port
# HTTPS_PROXY=http://proxy-server:port
# NO_PROXY=localhost,127.0.0.1

# Cloudflare Workers 代理（如果你设置了）
# CF_WORKER_URL=https://your-worker.your-subdomain.workers.dev

# 自定义 API Key（推荐经常更换）
# VALID_API_KEYS=key1,key2,key3

# 请求限制配置
# MAX_RETRIES=5
# RETRY_DELAY=2000
# REQUEST_TIMEOUT=60000

# IP 轮换配置（需要配合外部服务）
# IP_ROTATION_SERVICE=http://ip-rotation-service:8080
# IP_ROTATION_TOKEN=your-token